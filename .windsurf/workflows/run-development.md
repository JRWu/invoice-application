---
description: Deploy the application locally
---

1) Deploy the Python 3 Backend with:
# The virtual environment is located in the root directory of the project invoice-application/

source .venv/bin/activate  
cd src/backend/
python3 app.py

2) Create a new terminal instance, Deploy the Frontend with:

cd src/frontend
npm start